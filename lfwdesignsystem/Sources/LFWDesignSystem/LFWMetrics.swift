import CoreGraphics

/// Corner radii used across the family. Cards/buttons land on `card`; the
/// hero/feature surfaces on `surface`.
public enum LFWRadius {
    public static let chip: CGFloat = 8
    public static let card: CGFloat = 14
    public static let surface: CGFloat = 18
}
